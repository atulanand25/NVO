# Lab 11 — AI/ML for Network Engineering
# Installed with: pip install -r requirements.txt

# --- LLM + agent protocol -------------------------------------------------
anthropic>=0.30.0
mcp>=1.0.0

# --- RAG pipeline ---------------------------------------------------------
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
chromadb>=0.5.0
tiktoken>=0.7.0

# Default semantic embedding backend used by the RAG assistant.
# Pulls ~80 MB of model files from huggingface.co on first use.
sentence-transformers>=2.6.0

# --- Offline fallback embedding (used when sentence-transformers / HF are
#     unreachable). Also covers basic numerics.
scikit-learn>=1.5.0
numpy>=1.26.0

# --- CLI UX --------------------------------------------------------------
rich>=13.7.0
